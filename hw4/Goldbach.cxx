// every prime number equidistant between other primes

// p(x) true for all x

bool predicate = true;
for (all x)
  predicate = predicate // and some test on x

    // p(x) for some x 
    bool predicate = false;
for (all x)
  predicate = predicate or //some test on x
